Title: iMessage chats

Hyungsik: Basically the bottom line is we can keep the domain open and focus on deriving the right model (eg types of games). Basically the model will:
1. Extract key social problems attributes
2. Translate it into AI/ML problem characterisation (op function, constraints, etc)
3. Spit out the appropriate model

And run an eval using past AI4SI papers

+1 (919) 396-1815:	To clarify, spit out the appropriate model just means prompting a LLM here? Like are we building a predictive model or just using an LLM with our defined mapping +1 (617) 230-5528:	The latter. It cant be a predictive model cuz the problem space is way too complex	Structurally speaking
